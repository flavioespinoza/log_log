"main";_d71‍.x([["log",()=>log]]);let _;_d71‍.w("lodash",[["default",["_"],function(v){_=v}]]);let log;_d71‍.w("ololog",[["default",["log"],function(v){log=v}]]);let util;_d71‍.w("util",[["default",["util"],function(v){util=v}]]);// Using esm cli instead of bridge?
require('ansicolor').nice




const _log = {
    deep: data => {
        const options = {
            showHidden: false,
            depth: null,
            colors: true
        }
        const inspect = util.inspect(data, options)
        _d71‍.g.console.log(inspect)
    },
    info: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(` Info: ${msg} `.bgBlue.white)
    },
    error: msg => {
        log.lightYellow('ERROR: ', msg)
    },
    alert: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(` Alert: ${msg} `.bgYellow.black)
    },
    warn: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(` Warn: ${msg} `.bgMagenta.white)
    },
    blue: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgBlue.black)
    },
    red: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgRed.black)
    },
    green: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgGreen.black)
    },
    pink: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgLightRed.black)
    },
    yellow: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgLightYellow.white)
    },
    violet: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgLightMagenta.black)
    },
    cyan: msg => {
        if (typeof msg === 'object') {
            msg = _.toString(msg)
        }
        _d71‍.g.console.log(`${msg} `.bgCyan.black)
    },
    assert: (item, item_name) => {
        if (item) {
            let msg = ` SUCCESS: ${item_name} = ${item} `
            _d71‍.g.console.log(`ASSERT`, msg.bgCyan.black)
        } else {
            let msg = ` FAIL: ${item_name} = ${item} `
            _d71‍.g.console.log(`ASSERT`, msg.bgRed.black)
        }
    },
    timer: (method, method_name) => {
        _d71‍.g.console.time(`Timer ${method_name}()`)
        method()
        _d71‍.g.console.timeEnd(`Timer ${method_name}()`)
    }
}

_log.alert('Hello log_log')


_d71‍.d(_log);
